<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions commonly useful for FCM / network / notifications -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DindoriPranitYadnyiki">

        <!-- Main launcher activity (keep as before) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">

            <!-- launcher -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- DEEP LINK: custom scheme (example: app://dindori/puja?id=BOOKING_ID) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- scheme + host + optional pathPrefix -->
                <data android:scheme="app" android:host="dindori" android:pathPrefix="/puja" />
            </intent-filter>

            <!-- DEEP LINK: https domain (example: https://dindori.pranit/puja?id=BOOKING_ID)
                 Useful for autoVerify (app links) if you own the domain and have assetlinks.json configured.
                 If you don't own the domain, remove android:autoVerify or keep it false. -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="dindori.pranit" android:pathPrefix="/puja" />
            </intent-filter>
        </activity>

        <!-- Firebase Messaging service (handles incoming FCM data messages and builds notifications) -->
        <!-- Create a Kotlin class named MyFirebaseMessagingService that extends FirebaseMessagingService.
             Example: class MyFirebaseMessagingService : FirebaseMessagingService() { ... } -->
        <service
            android:name=".MyFirebaseMessagingService"
            android:exported="false"
            tools:ignore="ExportedService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- If you have a custom activity for InstantReceipt and want direct openings, add intent-filters similarly. -->
        <!-- Example placeholder (commented): -->
        <!--
        <activity android:name=".InstantReceiptActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="app" android:host="dindori" android:pathPrefix="/receipt" />
            </intent-filter>
        </activity>
        -->

        <!-- Firebase & google-services metadata typically added by google-services plugin; keep if present in your project -->
        <!--
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_ic_notification" />
        -->

    </application>

</manifest>
